# StadiumPulse AI Rules

Architecture is LOCKED.

Core Principle:
Verified Data → AI Intelligence → User

AI must never hallucinate stadium facts.

Never regenerate the project.

Never change folder structure.

Never change naming conventions.

Preserve component hierarchy.

Use Firebase Auth.

Use Firestore.

Primary AI: Gemini Flash
Fallback: Groq

Maps:
Leaflet + OpenStreetMap

Every AI response must include:
- Source
- Recommendation
- Reason

Staff approves alerts before fans receive them.